using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;

namespace Test
{
    class Program
    {
        static void Main(string[] args)
        {
            //while (true)
            //{
            //    string str = @"";
            //    Match col = Regex.Match(str, @"([a-zA-Zа-яА-Я0-9]{4,10})", RegexOptions.Singleline);
            //    Console.WriteLine(col.Success + "  " + str.Length.ToString());
            //}
            Console.WriteLine("Server start");
            Console.Read();
        }
    }
}
